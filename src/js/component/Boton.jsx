import React from 'react';

const Boton = (props) => {
    return (
        <div>
            <button type="button" class="btn btn-primary my-2">Find Out More!</button>
        </div>
    )
};

export default Boton;